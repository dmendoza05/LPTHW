from sys import argv

script, r1, r2, r3 = argv

print "This is script: %s" % script
print """We're only going to talk about red fruits!!
So far I only have 3 red fruits
They are %s, %s and %s""" % (r1, r2, r3)